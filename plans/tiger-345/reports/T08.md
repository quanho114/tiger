# T08 — Admin Login, Queue, Tables, & Operations State Transitions

Status: DONE
Ngày / agent / commit hoặc diff base: 2026-09-19 / Claude Code (Tiger 345 Full Implementation)

## Kết quả

- Hành vi đã chạy được:
  - Phân hệ quản trị Admin độc lập hoàn toàn (`/admin`), cách ly tuyệt đối khỏi layout khách hàng (Public Header, Footer, CartDrawer, ContactHub không render).
  - Admin Dashboard (`/admin`): hiển thị số liệu trực tiếp thời gian thực (đơn chờ, đang chế biến, đang giao, bàn có khách), công tắc tiếp nhận đơn, và danh sách đơn hàng gần đây.
  - Hàng đợi đơn hàng (`/admin/orders`): lọc đa chiều theo loại đơn (dine_in / delivery) và trạng thái (pending, confirmed, preparing, served, delivering, completed, rejected, cancelled).
  - Modal chi tiết đơn hàng (`AdminOrderDetailModal`): hiển thị snapshot món, đơn giá, số lượng, ghi chú khách, ghi chú nội bộ nhân viên (có kiểm soát concurrency version), lịch sử chuyển đổi trạng thái (audit timeline).
  - Máy trạng thái đơn hàng: tiếp nhận, chuyển chế biến, báo ra bàn / bắt đầu ship, hoàn tất đơn (bắt buộc kiểm tra `payment_status === 'paid'` theo Invariant V24), từ chối / hủy đơn kèm lý do.
  - Quản lý bàn & QR (`/admin/tables`): trực quan hóa trạng thái sử dụng bàn, hiển thị số lượng và tổng tiền đơn nợ chưa thanh toán, nút đóng phiên bàn chặn nếu còn đơn nợ chưa trả (Invariant V11), tạo và xem liên kết QR capability token kèm HMAC.
  - Cài đặt vận hành & thực đơn (`/admin/settings`): công tắc tiếp nhận đơn chung, đơn tại bàn, đơn delivery, đặt bàn trước; bảng chuyển đổi nhanh trạng thái Còn món / Hết món tức thì.

- Files/migrations/endpoints chính:
  - Backend: `supabase/functions/admin-api/index.ts`
  - Client API: `src/lib/api/client.ts`
  - Types: `src/features/admin/types.ts`
  - Admin Context: `src/features/admin/layout/AdminContext.tsx`
  - Admin Layout: `src/features/admin/layout/AdminLayout.tsx`
  - Admin Dashboard: `src/features/admin/dashboard/AdminDashboardPage.tsx`
  - Admin Orders: `src/features/admin/orders/AdminOrdersPage.tsx`, `src/features/admin/orders/AdminOrderDetailModal.tsx`
  - Admin Tables: `src/features/admin/tables/AdminTablesPage.tsx`
  - Admin Settings: `src/features/admin/settings/AdminSettingsPage.tsx`
  - Tests: `tests/integration/admin-orders.test.ts`, `tests/e2e/admin-queue.spec.ts`

- Acceptance criteria đã đạt:
  - Invariant V11: Bàn có đơn chưa thanh toán không thể đóng phiên (`close-visit` bị chặn ở cả backend RPC/API và UI disabled).
  - Invariant V24: Đơn hàng ở trạng thái `served` hoặc `delivering` chỉ được chuyển sang `completed` khi `payment_status === 'paid'`.
  - Invariant Concurrency: Mọi mutation (`transition`, `internal-note`, `settings`, `menu-items`) yêu cầu `expected_version` và trả về mã lỗi 409 `VERSION_CONFLICT` nếu dữ liệu đã bị sửa đổi từ phiên khác.
  - Invariant Isolation: Route `/admin/*` render trong layout riêng, không rò rỉ UI khách hàng hay cart drawer.

## Kiểm tra thực tế

| Command hoặc kịch bản | Environment | Exit/result | Evidence path |
|---|---|---|---|
| `npm run test:integration -- tests/integration/admin-orders.test.ts` | Local Supabase PostgreSQL (`127.0.0.1:54322`) | 0 (13/13 passed) | `tests/integration/admin-orders.test.ts` |
| `npm run test:e2e -- tests/e2e/admin-queue.spec.ts` | Local browser test (Chromium & Mobile Chrome) | 0 (10/10 passed) | `tests/e2e/admin-queue.spec.ts` |
| `npm run typecheck:server` | Local tsconfig.server.json | 0 (No errors) | Console output |
| `npm run lint` | Local oxlint | 0 (No errors) | Console output |
| `npm run typecheck` | Local tsconfig.json / tsc -b | 0 (No errors) | Console output |
| `npm run build` | Vite production build | 0 (Built successfully) | `dist/` |

## Thay đổi contract / quyết định

- Không làm lệch contract đã định nghĩa trong `05-api-contracts.md` và `06-data-model-and-migrations.md`.
- Bổ sung `AdminContext.tsx` tách biệt để đảm bảo tuân thủ React Fast Refresh và kiến trúc module một chiều.

## Chưa xong / rủi ro / external dependencies

- T08 hoàn tất trọn vẹn phần quản trị Admin và máy trạng thái đơn hàng.
- Rủi ro concurrency: Đã được bảo vệ bằng cột `version` và optimistic concurrency locks trên toàn bộ các bảng `orders`, `restaurant_settings`, `dining_tables`.
- Sẵn sàng bàn giao cho Task T09 (Đặt đơn hàng phía khách hàng: Cart, Quotes, Idempotent Order Submission).

## Rollback và bàn giao

- Code nằm trong nhánh `main` và phân hệ `/admin`. Để tạm dừng truy cập admin, có thể ngắt kết nối router `/admin` trong `src/App.tsx`.
- Task tiếp theo: T09 — Đặt món phía khách hàng (Dine-in QR & Delivery checkout, giỏ hàng, báo giá có chữ ký số, nộp đơn idempotent).
- File cần đọc: `plans/tiger-345/tasks/T09-order-placement.md`, `plans/tiger-345/05-api-contracts.md` (§Order Placement).
