# T13 — Admin menu/settings/QR và Storage

Status: DONE
Ngày / agent / commit hoặc diff base: 2026-09-20 / Claude / local workspace

## Kết quả

- Hành vi đã chạy được:
  - Cơ sở dữ liệu và Storage Migration (`20260920000004_admin_content_and_storage.sql`):
    - Khởi tạo Storage bucket `restaurant-media` công khai đọc (`public = true`), hạn mức dung lượng tệp 5MB (`5242880` bytes), cho phép các MIME types: `image/jpeg`, `image/png`, `image/webp`.
    - Bật RLS trên `storage.objects`: áp dụng chính sách `SELECT` công khai cho mọi người (`restaurant_media_public_read`), và các quyền `INSERT`, `UPDATE`, `DELETE` (`restaurant_media_admin_insert`, `restaurant_media_admin_update`, `restaurant_media_admin_delete`) chỉ dành riêng cho tài khoản quản trị viên thông qua `public.is_admin()`.
    - Triển khai stored procedure `replace_business_hours(p_expected_settings_version, p_hours, p_admin_id)` thực hiện thay thế toàn bộ lịch làm việc tuần atomically trong một giao dịch, kiểm tra trùng lặp khoảng thời gian giữa các ca (`HOURS_OVERLAP` HTTP 409), kiểm tra xung đột phiên bản lạc quan (`VERSION_CONFLICT` HTTP 409), và tự động tăng `restaurant_settings.version`.
    - Triển khai stored procedure `replace_business_closures(p_expected_settings_version, p_closures, p_admin_id)` thay thế danh sách ngày nghỉ đột xuất atomically, kiểm tra trùng lặp ngày/dịch vụ (`VALIDATION_ERROR` HTTP 422), kiểm tra xung đột phiên bản và cập nhật nhật ký kiểm toán `audit_logs`.
    - Cập nhật hàm `rotate_table_qr` tuân thủ Bất biến V07: vô hiệu hóa toàn bộ token cũ (`active = false`, `rotated_at = now()`), sinh token mới bảo mật (lưu `token_hash`), tự động tăng `dining_tables.version`, và nếu bàn có phiên phục vụ (`table_visits`) đang mở thì tăng `capability_epoch` để hủy quyền truy cập của các thiết bị dùng mã QR cũ.
  - Xử lý Backend API (`content-handlers.ts` & `admin-api`):
    - Quản lý danh mục (`/categories`) và món ăn (`/menu-items`): đầy đủ các thao tác CRUD, thay đổi giá (`price_vnd`), trạng thái hiển thị (`published`), trạng thái còn món (`available`), hỗ trợ tại bàn (`allow_dine_in`) và giao hàng (`allow_delivery`), gán nhãn đặc sản (`is_signature`), bán chạy (`is_bestseller`), món mới (`is_new`), và kiểm soát phiên bản OCC (`expected_version`).
    - Quản lý khu vực bàn (`/seating-areas`) và khu vực giao hàng (`/delivery-zones`): CRUD với `fee_vnd` và ngưỡng miễn phí giao hàng `free_threshold_vnd`.
    - Tải ảnh lên Storage (`POST /upload/image`): Giới hạn tệp 5MB (`FILE_TOO_LARGE` HTTP 413), kiểm tra định dạng magic bytes nhị phân thực tế của tệp (JPEG: `FF D8 FF`, PNG: `89 50 4E 47 0D 0A 1A 0A`, WebP: `RIFF....WEBP`), từ chối triệt để mã SVG, HTML hoặc script (`INVALID_FILE_TYPE` HTTP 422) ngăn ngừa tấn công Stored XSS.
    - Quản lý mã QR bàn: Hỗ trợ tạo mới bàn và xoay vòng mã QR (`POST /tables/:id/qr`), sinh token ngẫu nhiên 32-byte lưu mã băm SHA-256 trong cơ sở dữ liệu và chỉ trả raw token một lần duy nhất, hỗ trợ tải ảnh QR PNG và in thẻ bàn tent card.
  - Giao diện người dùng Quản trị viên (`AdminSettingsPage.tsx`, `AdminTablesPage.tsx`):
    - Tích hợp 4 tab quản lý chuyên sâu: "Vận Hành & Giờ Mở Cửa" (bật/tắt dịch vụ, thay đổi giờ hoạt động, thêm ngày nghỉ lễ), "Thực Đơn & Danh Mục" (tìm kiếm, lọc danh mục/trạng thái, form thêm/sửa món, tải ảnh), "Khu Vực & Phí Vận Chuyển", "Phản Hồi & Nhật Ký Kiểm Toán".
    - Trang Quản lý Bàn (`AdminTablesPage.tsx`): Hiển thị danh sách bàn theo trạng thái (bàn trống/có khách, nợ chưa thanh toán), thao tác xoay mã QR kèm cảnh báo Invariant V07, và modal tạo thẻ để bàn kèm mã QR in trực tiếp.

- Files/components/tests chính:
  - `supabase/migrations/20260920000004_admin_content_and_storage.sql`: Storage bucket `restaurant-media`, RLS storage policies, RPCs `replace_business_hours`, `replace_business_closures`, `rotate_table_qr`.
  - `supabase/functions/admin-api/content-handlers.ts`: Handlers quản lý catalog, settings, upload image với binary magic bytes sniffing, OCC và audit logs.
  - `supabase/functions/admin-api/index.ts`: Định tuyến admin API tới các content handlers.
  - `src/features/admin/settings/AdminSettingsPage.tsx`: Trang cài đặt hệ thống đa tab.
  - `src/features/admin/settings/OperationsHoursSection.tsx`: Tab quản lý giờ hoạt động và ngày nghỉ lễ.
  - `src/features/admin/settings/MenuContentSection.tsx`: Tab quản lý danh mục và thực đơn món ăn.
  - `src/features/admin/settings/AreasZonesSection.tsx`: Tab quản lý khu vực bàn và vùng giao hàng.
  - `src/features/admin/settings/FeedbackAuditSection.tsx`: Tab xem phản hồi concierge và nhật ký kiểm toán.
  - `src/features/admin/tables/AdminTablesPage.tsx`: Trang quản lý sơ đồ bàn, mở/đóng bàn và xoay mã QR.
  - `tests/integration/admin-content.test.ts`: Bộ 15 integration tests kiểm tra OCC, validation giờ/ngày nghỉ, magic bytes sniffing cho upload, RLS storage, và xoay QR bump version & epoch.
  - `tests/e2e/admin-content.spec.ts`: Bộ 3 Playwright tests (6 test cases đa viewport) kiểm thử duyệt tab cấu hình, thực đơn, khu vực, và thao tác xoay mã QR bàn.

- Acceptance criteria đã đạt:
  - Invariant V17 (Catalog & Setting Synchronization & Storage Protection): Đạt. Hạn mức 5MB, kiểm tra magic bytes nhị phân, chặn SVG/HTML, phân quyền RLS storage chuẩn xác.
  - Invariant V07 (QR Token Lifecycle & Table Integrity): Đạt. Chỉ lưu hash SHA-256, vô hiệu hóa mã cũ khi rotate, tự động bump `dining_tables.version` và `table_visits.capability_epoch`.
  - Invariant V24 (Optimistic Concurrency Control & Idempotency): Đạt. Các thao tác cập nhật cấu hình và catalog yêu cầu và kiểm tra `expected_version`, trả HTTP 409 `VERSION_CONFLICT` khi xung đột.

## Kiểm tra thực tế

| Command hoặc kịch bản | Environment | Exit/result | Evidence path |
|---|---|---|---|
| `npm run test:integration -- tests/integration/admin-content.test.ts` | local | Exit 0 (15 passed) | `tests/integration/admin-content.test.ts` |
| `npm run test:policies` | local | Exit 0 (17 passed) | `tests/policies/` |
| `npm run test:e2e -- tests/e2e/admin-content.spec.ts` | local | Exit 0 (6 passed) | `tests/e2e/admin-content.spec.ts` |
| `npm run typecheck:server` | local | Exit 0 | `tsconfig.server.json` |
| `npm run lint` | local | Exit 0 | `oxlint` |
| `npm run typecheck` | local | Exit 0 | `tsconfig.app.json` |
| `npm run build` | local | Exit 0 | `dist/` |

## Thay đổi contract / quyết định

- Tạo migration `20260920000004_admin_content_and_storage.sql` tích hợp trọn vẹn RPCs và Storage policies.
- Áp dụng kiểm tra magic bytes cấp nhị phân (Buffer inspection) cho luồng upload hình ảnh thay vì chỉ tin tưởng `Content-Type` header từ client.
- Xử lý atomic replacement cho lịch làm việc và ngày nghỉ, kiểm tra giao thoa khoảng giờ (overlap interval) ở cấp cơ sở dữ liệu để bảo đảm tính toàn vẹn dữ liệu.

## Chưa xong / rủi ro / external dependencies

- Task T13 hoàn thành toàn diện.
- Sẵn sàng tiến hành ngay Task T14: "Customer API: profile, addresses, order history, claim guest orders/reservations" theo chỉ thị liên tục của người dùng.

## Rollback và bàn giao

- Phiên bản settings và catalog sử dụng OCC nên việc sửa đổi sai cấu hình có thể khôi phục bằng cách gọi lại RPC với phiên bản tương ứng.
- Tiến hành thực thi Task T14 (`plans/tiger-345/tasks/T14-customer-api.md`).
