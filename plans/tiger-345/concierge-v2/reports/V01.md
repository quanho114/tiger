# V01 - Contract Freeze and Regression Baseline Report

Status: DONE

## Scope and dependencies

Completed V01 only. It had no dependencies and froze the contracts required by V02, V05, and V08. No successor task was started. The work preserves the existing legacy Concierge runtime while defining a separate, default-off V2 protocol boundary.

## Changes

- Added `supabase/functions/_shared/contracts/concierge-v2.ts`, the canonical Zod/TypeScript source for V2 request, completion, SSE, session lifecycle, CAS/idempotency, draft, handoff, tool-result, memory-repository, session-store, and content-free trace contracts. Durable session records store only a capability hash, actor binding, bounded transcript/summary/constraints/draft/pending suggestions/replay results, state version, and expiry metadata; server creation receives no client capability.
- Re-exported the V2 module from `supabase/functions/_shared/contracts/index.ts`.
- Added `tests/server/concierge-v2-contract.test.ts` with JSON/SSE equivalence, capability-safe errors and durable records, server-only session creation, bounded session state, expiry/conflict/idempotency distinction, ambiguous acceptance, food-safety handoff rejection, and repeat-handoff assertions.
- Added [V01-decisions.md](../V01-decisions.md), freezing the Upstash REST session-store choice, fail-closed behavior, durable bounded extraction rules, memory epoch policy, behavioral-order limits, food-safety behavior, export semantics, trace retention, budgets, and default-off compatibility sunset.
- Linked the decision record and this report from [README.md](../README.md). The primary-worktree contract, test, and V01 decision report received a source-level implementation review; this is V01 completion evidence only, not V18 independent release-review evidence.

No migration, feature flag implementation, runtime routing, browser persistence, cart mutation, order/payment/reservation logic, provider configuration, deployment, production cleanup, credentials, or live evaluation changed.

## Evidence

| Command | Exit | Outcome |
| --- | --- | --- |
| `npm run test:server -- --run tests/server/concierge-v2-contract.test.ts` | 0 | 12 V2 contract tests passed. |
| `npm run test:server -- --run tests/server/concierge-recommendation-safety.test.ts tests/server/concierge-llm-orchestration.test.ts` | 0 | 24 existing food-safety, orchestration, and SSE regression tests passed. |
| `npm run typecheck:server` | 0 | Shared server contracts compile under strict NodeNext settings. |
| `npm run lint -- --quiet` | 0 | Project lint passed. |
| `git diff --check` | 0 | No whitespace errors. |

The first RED test run failed only because the V2 module did not yet exist; after implementation, the focused test passed. The final implementation review inspected the actual primary-worktree V01 contract, contract test, decision record, requirements, task exit criteria, and report. It resolved the missing durable-session schema boundary by adding strict bounded session-record and server-create-input schemas before closing V01. This source review is not an independent V18 release review.

Acceptance coverage is contract-level only: A02/A03/A04 session lifecycle and expiry semantics, A05/A08 memory boundaries, A09/A10/A11/A12/A13 draft/handoff safety, A14/A16/A18 grounding/CAS/idempotency, and A21 content-free observability. These are not integration proofs.

## Remaining work

- V02 must verify real shared Redis CAS, TTL, TLS, and failure behavior with an isolated key prefix.
- V03 must remove V2 guest transcript/proposal/event persistence and wire protocol negotiation, expiry, and legacy-writer sunset into the real request path.
- V05/V06/V07 must implement schema, RLS, consent, epoch fencing, and durable extraction/behavioral retrieval.
- V08 must prove the authoritative catalog and allergen/cross-contact path.
- V13 must implement and test browser-cart atomic handoff application.
- PostgreSQL, Redis, browser, full unit/integration suites, production build, live provider evaluation, deployment, and production cleanup were not run. No mock test substitutes for those gates.
- V18 still requires an independent release review of the assembled implementation and V17 evidence; this report is not independent review evidence.

## Rollback

Revert `concierge-v2.ts`, its barrel export, V01-only test, and V01 documentation before consumers adopt the contract. No database or business data rollback exists because V01 did not change either.

## Handoff

- V02 implements `ConciergeV2SessionStore` using the documented Upstash REST adapter and must keep production fail-closed. It must generate the opaque capability server-side, persist only its hash using `ConciergeV2SessionRecordSchema`, and build records from `ConciergeV2SessionCreateInputSchema`.
- V05 implements `ConciergeV2MemoryRepository`; V08 supplies authoritative tool/catalog data satisfying the `ALLERGY_UNVERIFIED` boundary.
- V03 owns request-path integration, SSE/JSON negotiation, legacy persistence remediation, and feature-flag implementation.
- V13 owns the opaque handoff payload and atomic normal-cart applied-ID marker; the handoff contract intentionally never claims a browser cart mutation.
- Review focus: contract consumers must not duplicate DTOs, expose capabilities, reintroduce guest durable content, or turn V2 review/handoff into checkout/order/reservation confirmation.
