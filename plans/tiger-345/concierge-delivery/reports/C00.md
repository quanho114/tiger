# Report C00 — Baseline và Môi Trường Kiểm Thử Cô Lập

## Metadata

- Status: READY_FOR_REVIEW
- Timestamp/timezone: 2026-09-20T17:35:00+07:00
- Commit hoặc workspace snapshot/file hashes:
  - `tests/fixtures/test-db-guard.ts`
  - `tests/integration/test-db-guard.test.ts`
  - `tests/fixtures/auth-users.ts`
- Requirement IDs / acceptance IDs / dependencies: R01 / AT01 / Dependency: None
- Owned files và edits ngoài scope cần bảo toàn:
  - Owned: `tests/fixtures/test-db-guard.ts`, `tests/integration/test-db-guard.test.ts`, `tests/fixtures/auth-users.ts`
  - Preserved: Toàn bộ code `src/`, `supabase/`, `tests/` hiện có giữ nguyên vẹn.
- Environment: isolated DB `postgresql://postgres:postgres@127.0.0.1:54322/postgres` (port 54322), marker `public._test_isolation_marker` (`TIGER_345_TEST_ISOLATION_MARKER`), provider STUB cho deterministic tests.

## Baseline và phạm vi

1. **Inventory entrypoint → service → SQL → UI và đối chiếu F01–F10:**
   - **Entrypoint:** `supabase/functions/public-api/index.ts` (`/concierge/chat`, `/concierge/action`) & `supabase/functions/admin-api/index.ts` (`/admin/concierge/*`).
   - **Service/Runtime:** `supabase/functions/_shared/concierge/runtime.ts`, `state-machine.ts`, `persistence.ts`, `tools.ts`, `recommendation.ts`, `validator.ts`, `llm.ts`.
   - **SQL/Database:** `supabase/migrations/` (`20260919000001_core_schema.sql`, `20260919000002_customer_identity_and_policies.sql`, `20260919000004_order_engine_and_quotes.sql`, `20260920000001_delivery_order_engine.sql`).
   - **UI:** `src/features/concierge/components/ConciergeChatView.tsx`, `useConcierge.ts`, `src/store/CartProvider.tsx`.
   - **Đối chiếu Findings F01–F10:**
     - **F01 (C02):** Replay trả receipt cũ cho quote/payload khác -> Xác nhận tại `runtime.ts:470-508` khi `pending_action.status === 'completed'` không kiểm tra fingerprint hay reference quote ID.
     - **F02 (C02):** RPC và conversation CAS tách rời, race/lỗi sau commit -> Xác nhận `runtime.ts` gọi RPC `create_order`/`create_reservation` trước khi gọi `transitionConversationAsync`.
     - **F03 (C02):** Pending fingerprint/expiry/payload chưa bảo vệ đầy đủ confirmation -> Xác nhận `runtime.ts` chưa xác thực `content_fingerprint` của pending action khi confirm.
     - **F04 (C07):** Đặt bàn tự điền Quý khách và thời gian +2h -> Xác nhận tại `runtime.ts:1187-1190` (`customer_name: 'Quý khách'`, `bookingDate = new Date(Date.now() + 2 * 3600 * 1000)`).
     - **F05 (C05):** LLM adapter có nhưng chưa được runtime gọi -> Xác nhận `llm.ts` tách biệt và chưa được gọi từ `runtime.ts`.
     - **F06 (C08):** Customer favorites query dùng `is_available` thay `available` -> Xác nhận tại `tools.ts:401` (`SELECT ... mi.is_available`).
     - **F07 (C10):** Eval test metadata chưa assert side-effects thật -> Cần audit và bổ sung tại C10.
     - **F08 (C01):** Persistence còn fallback RAM khi DB lỗi -> Xác nhận tại `persistence.ts:44-48` (`catch (err) { const mem = inMemoryConversations.get(...) }`).
     - **F09 (C06):** Order chat chưa phát quote và thu thập đầy đủ qua UI -> Cần hoàn thiện tại C06.
     - **F10 (C03, C11):** Admin knowledge/config/approval và release evidence chưa đủ -> Cần triển khai tại C03.

2. **Test Isolation Guard (AT01):**
   - Đã triển khai `assertIsolatedTestDatabase(pool)` và `validateDatabaseTargetUrl(url)` trong `tests/fixtures/test-db-guard.ts`.
   - Bắt buộc kiểm tra host (`127.0.0.1`/`localhost`), port (`54322`), và marker bảng `public._test_isolation_marker`.
   - Từ chối mọi chuỗi kết nối trỏ đến remote/cloud host hoặc port tiêu chuẩn không cô lập (port 5432).
   - Tích hợp trực tiếp vào `seedFixtureUsers(pool)` và các test suite integration.

## Đường chạy thực tế

- Integration test runner (`npm run test:integration`) → `tests/integration/test-db-guard.test.ts` / `tests/fixtures/auth-users.ts` → `assertIsolatedTestDatabase(pool)` → Kiểm tra connection string & host/port → Kiểm tra marker `public._test_isolation_marker` → Cho phép chạy mutation / Từ chối trước write nếu không hợp lệ.

## Ma trận evidence

| Requirement/AT/case | Expected + forbidden side effects | Test/file | Artifact | Verdict |
|---|---|---|---|---|
| R01 / AT01 | Cho phép database test cô lập hợp lệ trên port 54322 và khởi tạo marker | `tests/integration/test-db-guard.test.ts` > `allows verified isolated test database on port 54322 and initializes marker` | Console output | PASSED |
| R01 / AT01 | Từ chối kết nối trỏ đến cloud/remote host trước khi kết nối (không có side effect write) | `tests/integration/test-db-guard.test.ts` > `rejects connection string pointing to remote/cloud host before any connection` | Console output | PASSED |
| R01 / AT01 | Từ chối kết nối trỏ đến port 5432 thông thường | `tests/integration/test-db-guard.test.ts` > `rejects connection string pointing to standard non-isolated port` | Console output | PASSED |
| R01 / AT01 | Từ chối mutation và ném lỗi khi marker bị tamper `is_isolated_test_db = false` | `tests/integration/test-db-guard.test.ts` > `refuses mutation and throws when database marker indicates is_isolated_test_db is false` | Console output | PASSED |

## Commands đã chạy

| Exact command/filter | Timestamp | Exit | Executed/pass/fail/skip | Artifact |
|---|---|---|---|---|
| `npx vitest run tests/integration/test-db-guard.test.ts --config vitest.integration.config.ts` | 2026-09-20T17:30:31+07:00 | 0 | 4 passed / 0 failed / 0 skipped | Vitest runner output |

## Negative tests và durability

- Negative test 1: `validateDatabaseTargetUrl` với URL trỏ tới `db.xyz.supabase.co:5432`, `aws-us-east-1.pooler.supabase.com:6543`, `production-db.tiger345.vn:54322` -> Ném `TestDatabaseSecurityError` trước bất kỳ kết nối mạng nào.
- Negative test 2: `validateDatabaseTargetUrl` với port `5432` thay vì `54322` -> Ném `TestDatabaseSecurityError`.
- Negative test 3: Marker `is_isolated_test_db = false` -> `assertIsolatedTestDatabase` từ chối thực thi và ném lỗi, không cho phép mutate.

## Self-review

- Severity: Không có issue nghiêm trọng.
- Target verification hoạt động tốt: chỉ cho phép 127.0.0.1/localhost và port 54322, có bảng marker xác thực.
- Không còn nguy cơ test suites ghi nhầm vào shared/production database.

## External gates và blockers

| Gate/phần việc | Trạng thái | Thiếu gì | Owner | Next action |
|---|---|---|---|---|
| G0 (DB test cô lập) | READY_FOR_REVIEW | Đã tạo guard và verified test suite | Coding Agent | Tiến hành C01 |

## Rollback và handoff

- Handoff sang C01: `assertIsolatedTestDatabase(pool)` có sẵn tại `tests/fixtures/test-db-guard.ts` để các task C01–C11 tái sử dụng.
- Rollback: Nếu cần vô hiệu, bỏ import tại `auth-users.ts`, tuy nhiên đây là guardrail an toàn bắt buộc theo AT01.

## Reviewer only

<!-- Để trống cho Reviewer -->
- Verdict: 
- Reviewer note:
